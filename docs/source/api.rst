API Reference
=============

The API reference is generated automatically from Doxygen comments embedded in
the C source code. Build it with ``cmake --build build --target docs_api`` and
open ``build/docs/api/html/index.html``.
